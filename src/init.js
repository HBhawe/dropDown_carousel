/**
 * The `initialise` function adds the "hidden" class to a dropdown item to hide it.
 * @param dropdownItem - The `dropdownItem` parameter is a reference to an HTML element that represents
 * a dropdown item in a web page.
 */
export const initialise = function (dropdownItem) {
  dropdownItem.classList.add("hidden");
};
